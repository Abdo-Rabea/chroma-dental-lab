import styled from 'styled-components';
//* customized for deposites

const ButtonGroup = styled.div`
  display: flex;
  gap: 1rem;
  justify-content: space-around;
  button {
    flex-grow: 1;
    margin-top: 1rem;
    margin-bottom: 0.6rem;
  }
`;

export default ButtonGroup;
